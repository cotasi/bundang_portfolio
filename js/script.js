window.addEventListener("DOMContentLoaded",function(){
    const bdy = document.body;
    /* bdy.classList.remove("popupdiv"); */
    const btn = document.querySelector("button");
    btn.onclick=()=> {
        bdy.classList = "";
    }
    
});